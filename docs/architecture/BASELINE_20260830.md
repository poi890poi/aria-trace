# Software baseline - 2026-08-30

## Purpose

This baseline freezes the state observed before extracting existing calibration,
mapping, localization, tracking, replay, or Workbench algorithms into the layered
architecture. A refactor slice must not claim a clean full-suite baseline by hiding
these results or weakening their assertions.

## Environment

```text
Python: 3.12.10
OpenCV: 5.0.0
NumPy: 2.5.2
Runtime: .tools/standalone-release-py31210/Scripts/python.exe
Command: python -m unittest discover -s tests -v
```

## Result

```text
Tests run: 388
Passed: 386
Failed: 2
Elapsed: 81.444 seconds
Hardware opened: no
```

Both failures reproduce when run individually in the same environment.

### Map-stitch oriented-gradient polarity

```text
Test:
  test_map_stitching.MapStitchingTests.
  test_oriented_gradient_zncc_prefers_matching_edge_polarity

Expected location: (5, 20)
Actual location:   (50, 20)
```

The deterministic synthetic scene contains matching- and opposite-polarity edge
candidates. The current matcher selects the opposite candidate under this runtime.
This is an algorithm or dependency-compatibility investigation, not authorization
to weaken the assertion.

### Rig feature repeatability

```text
Test:
  test_rig_calibration.RigImageQualityTests.
  test_ground_truth_feature_metrics_and_mma

Expected 1 px repeatability: > 0.90
Actual 1 px repeatability:     0.7397899649941657
```

The result is deterministic on the synthetic transform. The rig-calibration owner
must determine whether OpenCV 5 feature behavior, correspondence construction, or
the threshold contract is responsible. This baseline does not alter rig code or its
acceptance criterion.

## Refactor foundation verification

The focused foundation suite contains component contracts, architecture boundaries,
legacy frame adapters, generic latest-value pumping, invocation traces, and backup
restore behavior. It passes 18/18 tests. The frame-pump suite additionally passed in
10 consecutive fresh processes after replacing a fixed-delay assertion with an
observable progress condition.

## Gate

Until the two baseline failures are diagnosed independently:

- refactor commits must keep the failure count from increasing;
- focused tests for moved components must pass completely;
- map-stitch or rig-calibration behavior changes must address their own failure in a
  separate evidence-backed commit;
- the full suite may be reported only as `386 passed, 2 known baseline failures`,
  never as a pass.
